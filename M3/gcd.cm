int y;

int gcd(int u, int v) {
  output(u);
  output(v);
  if (v == 0) return u;
  else{
    int w;
    w = u - u/v*v;
    return gcd(v, w);
  }
}

void main(void) {
  int x;
  x = input();
  y = 10;
  output(gcd(x, y));
}
